"""Pinned config per PLAN.md's API contract section. Placeholders are explicit — do not fabricate real values.

TODO(team): PLAN.md "Next 60 minutes" item 5 — verify Bedrock model access + Polly voices in the
team AWS account, then replace the two TODO_ values below.
"""

AWS_REGION = "TODO_SET_AWS_REGION"
BEDROCK_MODEL_ID = "TODO_SET_MODEL_ID"

TIMEOUT_SECONDS = 10
MAX_RETRIES = 1
